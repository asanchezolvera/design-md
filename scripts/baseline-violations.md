# Baseline violations — 2026-05-13

Generated by: `node scripts/validate-design.mjs --all`

All 8 brands fail validation. This is expected pre-migration.
Each violation maps to a rename in `docs/canonical-token-migration.md` Phase 2/3 tables.

```
skills/activated-you/DESIGN.md: FAIL — 18 violation(s) across 28 tokens
  color-named:
    - button-accent: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-hover: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-active: color-named segment `accent` — tokens must be role-based, not color-named
    - badge-accent: color-named segment `accent` — tokens must be role-based, not color-named
  unknown-prefix:
    - product-tile: unknown component prefix `product` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - product-tile-hover: unknown component prefix `product` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - banner-trust: unknown component prefix `banner` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  missing-canonical-role:
    - button-inverse: missing required canonical role
    - card-inverse: missing required canonical role
    - badge: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-inverse: missing required canonical role
skills/bhmd/DESIGN.md: FAIL — 18 violation(s) across 23 tokens
  surface-context-suffix:
    - button-dark: `-dark` surface-context suffix — use `-inverse`
    - card-dark: `-dark` surface-context suffix — use `-inverse`
  bad-prefix:
    - navigation: non-canonical prefix `navigation` — use `nav`
  unknown-prefix:
    - modal: unknown component prefix `modal` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  missing-canonical-role:
    - button-ghost: missing required canonical role
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-inverse: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/debt-clear-usa/DESIGN.md: FAIL — 19 violation(s) across 25 tokens
  color-named:
    - button-cta: color-named segment `cta` — tokens must be role-based, not color-named
    - button-cta-hover: color-named segment `cta` — tokens must be role-based, not color-named
    - button-cta-active: color-named segment `cta` — tokens must be role-based, not color-named
  surface-context-suffix:
    - card-dark: `-dark` surface-context suffix — use `-inverse`
    - nav-bar-dark: `-dark` surface-context suffix — use `-inverse`
  bad-prefix:
    - nav-bar: non-canonical prefix `nav-bar` — use `nav`
    - nav-bar-dark: non-canonical prefix `nav-bar` — use `nav`
  missing-canonical-role:
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-feature: missing required canonical role
    - card-inverse: missing required canonical role
    - badge: missing required canonical role
    - badge-primary: missing required canonical role
    - badge-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/driven-entrepreneur/DESIGN.md: FAIL — 34 violation(s) across 28 tokens
  color-named:
    - button-forest: color-named segment `forest` — tokens must be role-based, not color-named
    - button-forest-hover: color-named segment `forest` — tokens must be role-based, not color-named
    - button-forest-active: color-named segment `forest` — tokens must be role-based, not color-named
    - button-terracotta: color-named segment `terracotta` — tokens must be role-based, not color-named
    - button-terracotta-hover: color-named segment `terracotta` — tokens must be role-based, not color-named
    - card-beige: color-named segment `beige` — tokens must be role-based, not color-named
    - card-forest: color-named segment `forest` — tokens must be role-based, not color-named
  surface-context-suffix:
    - button-outline-dark: `-dark` surface-context suffix — use `-inverse`
    - input-dark: `-dark` surface-context suffix — use `-inverse`
  unknown-prefix:
    - section-beige: unknown component prefix `section` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - section-forest: unknown component prefix `section` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - section-spotlight: unknown component prefix `section` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - section-gold-tint: unknown component prefix `section` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - eyebrow: unknown component prefix `eyebrow` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - highlight-keyword: unknown component prefix `highlight` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  bad-prefix:
    - tag: non-canonical prefix `tag` — use `badge`
  missing-canonical-role:
    - button-secondary: missing required canonical role
    - button-ghost: missing required canonical role
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-feature: missing required canonical role
    - card-inverse: missing required canonical role
    - badge: missing required canonical role
    - badge-primary: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-disabled: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/gundry-md/DESIGN.md: FAIL — 21 violation(s) across 26 tokens
  surface-context-suffix:
    - button-dark: `-dark` surface-context suffix — use `-inverse`
    - card-dark: `-dark` surface-context suffix — use `-inverse`
  unknown-prefix:
    - textarea: unknown component prefix `textarea` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - trust-bar: unknown component prefix `trust` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - modal: unknown component prefix `modal` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  bad-prefix:
    - navigation: non-canonical prefix `navigation` — use `nav`
  color-named:
    - badge-coral: color-named segment `coral` — tokens must be role-based, not color-named
  missing-canonical-role:
    - button-ghost: missing required canonical role
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-inverse: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/power-life/DESIGN.md: FAIL — 31 violation(s) across 34 tokens
  color-named:
    - button-accent: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-hover: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-active: color-named segment `accent` — tokens must be role-based, not color-named
    - button-vital: color-named segment `vital` — tokens must be role-based, not color-named
    - button-vital-hover: color-named segment `vital` — tokens must be role-based, not color-named
    - badge-vital: color-named segment `vital` — tokens must be role-based, not color-named
    - badge-spark: color-named segment `spark` — tokens must be role-based, not color-named
  surface-context-suffix:
    - card-dark: `-dark` surface-context suffix — use `-inverse`
    - navbar-dark: `-dark` surface-context suffix — use `-inverse`
  bad-prefix:
    - navbar: non-canonical prefix `navbar` — use `nav`
    - navbar-dark: non-canonical prefix `navbar` — use `nav`
  unknown-prefix:
    - banner-promo: unknown component prefix `banner` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-info: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-success: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-warning: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-error: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  missing-canonical-role:
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-feature: missing required canonical role
    - card-inverse: missing required canonical role
    - badge: missing required canonical role
    - badge-primary: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/roundhouse-provisions/DESIGN.md: FAIL — 36 violation(s) across 37 tokens
  color-named:
    - button-ember: color-named segment `ember` — tokens must be role-based, not color-named
    - button-ember-hover: color-named segment `ember` — tokens must be role-based, not color-named
    - button-ember-active: color-named segment `ember` — tokens must be role-based, not color-named
    - button-sky: color-named segment `sky` — tokens must be role-based, not color-named
    - button-sky-hover: color-named segment `sky` — tokens must be role-based, not color-named
    - button-saddle: color-named segment `saddle` — tokens must be role-based, not color-named
    - button-saddle-hover: color-named segment `saddle` — tokens must be role-based, not color-named
    - card-ember: color-named segment `ember` — tokens must be role-based, not color-named
    - badge-ember: color-named segment `ember` — tokens must be role-based, not color-named
    - badge-sky: color-named segment `sky` — tokens must be role-based, not color-named
    - badge-saddle: color-named segment `saddle` — tokens must be role-based, not color-named
  surface-context-suffix:
    - card-dark: `-dark` surface-context suffix — use `-inverse`
    - navbar-dark: `-dark` surface-context suffix — use `-inverse`
  bad-prefix:
    - navbar: non-canonical prefix `navbar` — use `nav`
    - navbar-dark: non-canonical prefix `navbar` — use `nav`
  unknown-prefix:
    - banner-promo: unknown component prefix `banner` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-info: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-success: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-warning: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - callout-error: unknown component prefix `callout` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  missing-canonical-role:
    - button-inverse: missing required canonical role
    - card-elevated: missing required canonical role
    - card-feature: missing required canonical role
    - card-inverse: missing required canonical role
    - badge: missing required canonical role
    - badge-primary: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
skills/terra-mare/DESIGN.md: FAIL — 21 violation(s) across 28 tokens
  color-named:
    - button-accent: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-hover: color-named segment `accent` — tokens must be role-based, not color-named
    - button-accent-active: color-named segment `accent` — tokens must be role-based, not color-named
  surface-context-suffix:
    - card-dark: `-dark` surface-context suffix — use `-inverse`
  unknown-prefix:
    - textarea: unknown component prefix `textarea` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
    - modal: unknown component prefix `modal` (expected one of button, card, badge, input, nav; otherwise document as a prose recipe)
  bad-prefix:
    - navigation: non-canonical prefix `navigation` — use `nav`
  missing-canonical-role:
    - button-ghost: missing required canonical role
    - card-elevated: missing required canonical role
    - card-feature: missing required canonical role
    - card-inverse: missing required canonical role
    - badge-success: missing required canonical role
    - badge-warning: missing required canonical role
    - badge-error: missing required canonical role
    - badge-info: missing required canonical role
    - input-error: missing required canonical role
    - input-inverse: missing required canonical role
    - nav: missing required canonical role
    - nav-link: missing required canonical role
    - nav-link-active: missing required canonical role
    - nav-inverse: missing required canonical role
```
